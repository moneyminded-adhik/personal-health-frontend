# Server config
PROXY_PORT=8083
FASTAPI_HOST=localhost
FASTAPI_PORT=8082

# Set to your Wi-Fi IP so the Android app can connect
# Run `ipconfig` (Windows) or `ifconfig` (Mac/Linux) to find it
PC_IP=172.17.105.97

# App info injected into pages
APP_ENV=development
